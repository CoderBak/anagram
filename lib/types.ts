// lib/types.ts — internal types (Appendix A).

export const MARK_ATTR = "data-pangram"; // values: "host" | "scored"
export type Lane = "viewport" | "near" | "background";
export type ScanEpoch = number;
export { type Unit } from "./dom/text";
